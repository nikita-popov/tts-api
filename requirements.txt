flasgger==0.9.7.1
Flask==3.1.3
gunicorn==25.3.0
# kokoro-onnx>=0.4.6 requires phonemizer-fork (not phonemizer).
# phonemizer-fork and phonemizer are separate packages and coexist fine.
kokoro-onnx==0.4.9
mcp[cli]==1.27.0
misaki[ja,zh]==0.7.4
onnxruntime==1.25.0
# phonemizer (bootphon) is used by app/piper.py via EspeakBackend
phonemizer==3.3.0
# phonemizer-fork is pulled in by kokoro-onnx automatically,
# listed here explicitly for clarity
phonemizer-fork>=3.3.2
scipy>=1.11
sounddevice==0.5.5
torch>=2.0
