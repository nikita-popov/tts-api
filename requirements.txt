flasgger==0.9.7.1
Flask==3.1.3
gunicorn==25.3.0
# 0.4.5 is the last version where espeak integration works correctly.
# Newer versions require phonemizer-fork which conflicts with phonemizer.
kokoro-onnx==0.4.5
mcp[cli]==1.27.0
misaki[ja,zh]==0.7.4
onnxruntime==1.25.0
# phonemizer is used by app/piper.py (EspeakBackend for ru-piper)
phonemizer==3.3.0
scipy>=1.11
sounddevice==0.5.5
torch>=2.0
